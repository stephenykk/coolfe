{
  "scripts": {
    "build": "node build && npm run commit",
    "commit": "git add . && git commit -m \"rebuild index.html\" && git push"
  },
  "devDependencies": {
    "opn": "^5.4.0"
  },
  "dependencies": {
    "exceljs": "^4.3.0",
    "markdown-html": "0.0.8"
  }
}
