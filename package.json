{
  "scripts": {
    "build": "node build",
    "commit": "git add . && git commit -m 'rebuild index.html'"
  },
  "devDependencies": {
    "opn": "^5.4.0"
  },

  "dependencies": {
    "mpvue": "^2.0.5"
  }
}
