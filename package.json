{
  "scripts": {
    "pull": "git pull origin master",
    "push": "git push origin master && git push gitee master",
    "p": "npm run pull && npm run push"
  },
  "devDependencies": {
    "opn": "^5.4.0"
  },
  "dependencies": {
    "exceljs": "^4.3.0",
    "express": "^4.18.1",
    "markdown-html": "0.0.8",
    "prompt": "^1.3.0"
  }
}
